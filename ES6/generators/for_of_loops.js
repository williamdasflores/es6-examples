const colors = ['red', 'blue', 'white', 'green'];

for (let color of colors) {
    console.log(color);
}

const numbers = [5, 12, 5, 0, 10];
let sum = 0;
for (let number of numbers) {
    sum += number;
}
console.log(sum);
